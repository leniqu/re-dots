################
### MONITORS ###
################

# See https://wiki.hypr.land/Configuring/Monitors/
monitor=HDMI-A-1,1920x1080@165,0x0,1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = nautilus
$menu = wofi --show drun --allow-images --show-icons --style /home/lenique/.config/wofi/style.css --width 30% --lines 6 --no-actions

#################
### AUTOSTART ###
#################
exec-once = swayosd-server
exec-once = waybar
exec-once = swww-daemon # Запускает движок анимаций
exec-once = swww img ~/Pictures/wallpapers/PC.jpg # Ставит начальные обои
exec-once = wal -R # Применяет цвета (Pywal)
exec-once = pywalfox start
exec-once = swaync -c ~/.config/swaync/config.json
exec-once = eww daemon
exec-once = eww open clock

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hypr.land/Configuring/Environment-variables/
env = COLORTERM,truecolor
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = XCURSOR_THEME,Bibata-Modern-Ice

###################
### PERMISSIONS ###
###################

# See https://wiki.hypr.land/Configuring/Permissions/


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 3
    gaps_out = 5
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)
    resize_on_border = false
    allow_tearing = false
    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 6
        passes = 3
        new_optimizations = on
        noise = 0.02
        contrast = 1.0
        brightness = 0.9
        vibrancy = 0.5
    }
}

# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
    enabled = yes

    # 1. Оригинальные кривые Безье из HyDE
    bezier = wind, 0.05, 0.9, 0.1, 1.05
    bezier = winIn, 0.1, 1.1, 0.1, 1.1
    bezier = winOut, 0.3, -0.3, 0, 1
    bezier = liner, 1, 1, 1, 1

    # Из твоего конфига
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    # 2. Оригинальные настройки анимации окон из HyDE (стиль slide)
    animation = windows,     1, 6, wind, slide
    animation = windowsIn,   1, 6, winIn, slide
    animation = windowsOut,  1, 5, winOut, slide
    animation = windowsMove, 1, 5, wind, slide

    # Твои стандартные затухания
    animation = fadeIn,        1,     1.73, almostLinear
    animation = fadeOut,       1,     1.46, almostLinear
    animation = fade,          1,     3.03, quick
    
    # Твои слои (панели, рофи, уведомления)
    animation = layers,        1,     3.81, easeOutQuint
    animation = layersIn,      1,     4,    easeOutQuint, fade
    animation = layersOut,     1,     1.5,  linear,       fade
    animation = fadeLayersIn,  1,     1.79, almostLinear
    animation = fadeLayersOut, 1,     1.39, almostLinear
    
    # Зум
    animation = zoomFactor,    1,     7,    quick
    
    # Ворксейсы
    animation = workspaces,    1,     5,    easeOutQuint, slide
}

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    # ИСПРАВЛЕНО: Строка pseudotile = true полностью удалена, так как опция больше не существует в коде Hyprland
    preserve_split = true 
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 
    disable_hyprland_logo = false 
}


#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us,ru
    kb_variant =
    kb_model =
    kb_options = grp:alt_shift_toggle
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 

    touchpad {
        natural_scroll = false
    }
}

# See https://wiki.hypr.land/Configuring/Gestures

device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

$mainMod = SUPER 

# Перезагрузка конфига Hyprland + перезапуск Waybar
bind = SUPER, Escape, exec, eww close player
bind = SUPER_SHIFT, R, exec, hyprctl reload 
bind = $mainMod, L, exec, hyprlock
bind = $mainMod, W, exec, ~/.config/hypr/scripts/wallselect.sh
bind = $mainMod, E, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, T, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle

# ИСПРАВЛЕНО: togglesplit теперь вызывается через менеджер макетов layoutmsg
bind = $mainMod, J, layoutmsg, togglesplit # dwindle

bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
# (ИСПРАВЛЕНО: Восстановлен оборванный фрагмент кода)
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow


# Переключение игрового режима через встроенный скрипт
# Кнопка включения/выключения самодельного игрового режима
bind = $mainMod, F1, exec, ~/.config/hypr/gamemode.sh
bind = SUPER, G, exec, ~/.config/hypr/scripts/gamemode.sh
bind = ,Caps_Lock, exec, /home/lenique/.config/hypr/scripts/capslock_notify.sh
